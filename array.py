from array import array

arr=array('i',[1,2,3,4,5,6])

#array is more performance than the list
#In the array we defined the type code
#So the perfromance is high than the normal list in python
print(arr)
print(arr[2])